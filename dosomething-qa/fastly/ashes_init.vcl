# This is an array of campaigns that should be routed to Ashes. Fastly's
# VCL supports dictionaries, but not arrays... hence an array of all "true"!
table ashes_campaigns {
  "shelter-pet-pr": "true"
}
